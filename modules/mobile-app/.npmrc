# @jitsi/react-native-sdk pulls in react-native-dialog via a git URL.
# The exotic-subdep block is disabled here so pnpm can resolve it.
exotic-subdeps-blocklist-delay=false
block-exotic-subdeps=false
